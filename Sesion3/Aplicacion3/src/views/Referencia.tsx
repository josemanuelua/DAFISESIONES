import { NavLink } from "react-router-dom";

const Referencia = ():React.JSX.Element =>{
    return(
        <div id="referencia">
            <h2>Página de referencias</h2>
        </div>
    );
};

export default Referencia