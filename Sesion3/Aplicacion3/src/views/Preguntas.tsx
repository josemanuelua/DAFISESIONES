import { NavLink } from "react-router-dom";

const Preguntas = ():React.JSX.Element => {
    return(
        <div id="preguntas">
            <h2>Página preguntas frecuentes</h2>
        </div>
    );
};


export default Preguntas